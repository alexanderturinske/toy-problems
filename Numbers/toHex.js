'use strict';
/**
   Write a function that converts a number to hex
   Example
   (7).toString(16)
   (10).toString(16)
**/

const toHex = () => {

};

console.log(toHex() === EXPECTED_OUTCOME);
// -> EXPECTED_OUTCOME
console.log(toHex() === EXPECTED_OUTCOME);
// -> EXPECTED_OUTCOME
console.log(toHex() === EXPECTED_OUTCOME);
// -> EXPECTED_OUTCOME
console.log(toHex() === EXPECTED_OUTCOME);
// -> EXPECTED_OUTCOME

module.exports = {
  toHex,
};