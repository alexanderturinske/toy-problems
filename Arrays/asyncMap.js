'use strict';
/**
 * Description
**/

const asyncMap = () => {

};

console.log(asyncMap() === EXPECTED_OUTCOME);
// -> EXPECTED_OUTCOME
console.log(asyncMap() === EXPECTED_OUTCOME);
// -> EXPECTED_OUTCOME
console.log(asyncMap() === EXPECTED_OUTCOME);
// -> EXPECTED_OUTCOME
console.log(asyncMap() === EXPECTED_OUTCOME);
// -> EXPECTED_OUTCOME

module.exports = {
  asyncMap,
};
