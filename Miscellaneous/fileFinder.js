'use strict';
/**
create a simple command-line tool (using js/node) that searches inside a directory for all text files that have a specific word
grep?
process.exec?
pass arguments?
**/

const fileFinder = () => {

};

console.log(fileFinder() === EXPECTED_OUTCOME);
// -> EXPECTED_OUTCOME
console.log(fileFinder() === EXPECTED_OUTCOME);
// -> EXPECTED_OUTCOME
console.log(fileFinder() === EXPECTED_OUTCOME);
// -> EXPECTED_OUTCOME
console.log(fileFinder() === EXPECTED_OUTCOME);
// -> EXPECTED_OUTCOME

module.exports = {
  fileFinder,
};
